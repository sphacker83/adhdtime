# Mobile-First UI Image Parity Tasks

Last Updated: 2026-02-28

## Phase 0: 문서화/기준선 🟡 IN PROGRESS
- [x] Dev Docs 3종 생성 (`plan/context/tasks`)
- [x] 레퍼런스 이미지 경로 확정 (`/docs/ui/main_ui.png`, `/docs/ui/ui.png`, `/docs/ui/add_ui.png`)
- [x] 신규 요구사항 6개 문서 반영
- [ ] 390px 기준 현재 UI 스크린샷 캡처
- [x] 섹션별 갭 체크리스트 작성 (헤더/Status Card/입력/퀘스트/탭바)

Acceptance:
- [x] 우선순위가 있는 갭 목록이 문서화되어 구현 순서가 확정된다.

## Phase 1: 모바일 셸 재구성 ✅ COMPLETE
- [ ] 헤더 `main_ui` 완전 정합
- [x] 헤더/상태/입력/퀘스트/탭바 순서로 렌더 트리 재배치
- [x] safe-area 패딩 + 하단 탭바 고정 + 헤더 고정 반영
- [x] 메인 내용만 스크롤되도록 영역 분리

Acceptance:
- [x] 390px 기준 1스크린 정보 구조가 레퍼런스와 동일한 순서다.
- [x] 헤더/탭바 고정 상태에서 본문만 스크롤된다.

## Phase 2: Status Card 정합 ✅ COMPLETE
- [x] 타이틀/캐릭터 상태 사이 메시지 제거
- [x] 레벨/진행률 배지 위치와 타이포 계층 조정
- [x] 레이더 카드 라벨/그리드/폴리곤 스타일 보정
- [x] 카드 그림자/라운드/여백을 레퍼런스 톤으로 통일

Acceptance:
- [x] Status Card가 시각적 중심 섹션으로 인지된다.
- [x] 타이틀/상태 전환 영역에서 불필요 메시지가 노출되지 않는다.

## Phase 3: 입력 + AI 액션 클러스터 ✅ COMPLETE
- [x] 무지성 청킹 영역에 단일 플로팅 생성 버튼 배치
- [x] 입력 우측 STT 버튼 배치
- [x] 생성 버튼 라벨을 `AI 퀘스트 생성`으로 고정
- [x] STT 상태 배지/오류/fallback 문구 우선순위 정리
- [x] 퀘스트/AI 생성 일정 표기를 `오늘/내일/D-##`로 정규화하고 엄격 파싱 적용
- [ ] 엄지 조작 기준 탭 영역(44x44+) 검증

Acceptance:
- [x] `입력 -> AI 퀘스트 생성` 흐름이 2탭 이내로 완료된다.
- [x] STT 미지원 환경 fallback이 깨지지 않는다.
- [x] AI 생성 경로 일정 파싱 규칙이 수동 생성과 충돌하지 않는다.

## Phase 4: 현재/대기 퀘스트 + 모달 ✅ COMPLETE
- [x] 리스트 아이템 기본 카드 UI(체크/아이콘/타이틀) 보정
- [x] 확장 카드 타이머 타이포와 보조 라벨 배치
- [x] 시작/일시정지/완료 3개 CTA 색상/크기/배치 정합
- [x] 진행중 퀘스트에 다음 미션 표시 (`/docs/ui/add_ui.png` 참조)
- [x] 대기중 퀘스트에서 완료 항목 제외
- [x] 실행 중 청크 증감 버튼을 한 줄(`-5/-1/+1/+5`)로 통일
- [x] 퀘스트/청크 변경 경로에서 시작/마감 일정 자동 정규화 및 표시 동기화

Acceptance:
- [x] 확장 카드 구조가 레퍼런스와 유사한 시각 계층을 가진다.
- [x] 진행중 퀘스트에 다음 미션이 안정적으로 노출된다.
- [x] 대기중 목록에 완료 항목이 노출되지 않는다.

## Phase 5: 탭바 + 반응형/회귀 검증 ⬜ NOT STARTED
- [ ] 하단 탭바 아이콘/라벨/활성 상태 스타일 정리
- [ ] `390px`, `430px`, `768px+` 반응형 검증
- [ ] 색 대비/키보드 포커스/터치 타겟 접근성 확인

Acceptance:
- [ ] 모바일 우선 UI가 화면 크기 변화에도 깨지지 않는다.
- [ ] 핵심 플로우(생성->시작->일시정지->완료)가 정상 동작한다.

## Section-Based Image Parity Checklist
### 1) 상단 타이틀 바 (`/docs/ui/main_ui.png`)
- [ ] 헤더가 레퍼런스와 완전 정합(배치/간격/강조)
- [x] 좌우 정보 블록의 수직 기준선 정렬
- [x] 좌측 브랜드 블록(타이틀/레벨) 타이포 계층 유지
- [x] 우측 `75%` 진행 정보의 시각 강조(링/텍스트 대비) 유지

### 2) 캐릭터 상태 (`/docs/ui/main_ui.png`)
- [x] 타이틀/캐릭터 상태 사이 메시지 제거 확인
- [x] 카드 라운드/그림자/패딩이 기준 이미지와 유사
- [x] 레이더 폴리곤 색상 레이어와 축 라벨 위치 재현
- [x] 캐릭터 일러스트와 차트 사이 간격 균형 유지

### 3) 태스크 청킹 (`/docs/ui/ui.png`)
- [x] 단일 플로팅 생성 버튼 구조 반영
- [x] 입력 우측 STT 배치 반영
- [x] 생성 버튼명 `AI 퀘스트 생성` 적용
- [x] 입력창/STT/CTA가 한 손 조작 영역에서 2탭 이내 동선
- [x] 한국어+영문 병기 라벨의 줄바꿈/오버플로우 안정화
- [x] 입력 placeholder와 보조 아이콘 대비 확보

### 4) 현재 퀘스트 (`/docs/ui/main_ui.png`)
- [x] 타이머가 카드의 1순위 시각 계층으로 인지
- [x] 3개 CTA의 색상/크기/간격/라운드 일치
- [x] 진행중 퀘스트의 다음 미션 노출 (`/docs/ui/add_ui.png` 기준)
- [x] 보조 알림 텍스트가 보조 계층으로 유지

### 5) 대기중 퀘스트 (`/docs/ui/main_ui.png`)
- [x] 완료 항목 제외 규칙 적용
- [x] 행 높이/아이콘 크기/텍스트 간격을 압축형 리스트 톤으로 유지
- [x] 메타 정보(`시작/마감/소요`) 가독성 유지
- [x] 체크/확장 인터랙션의 충돌(이벤트 버블링) 방지

### 6) 퀘스트 추가/편집 (`/docs/ui/add_ui.png`)
- [x] 하단 시트형 오버레이 계층과 배경 분리
- [x] 입력 -> 시간카드 -> 프리셋 -> 생성 CTA 순서 고정
- [x] 소요시간 변경 시 시작시간 대비 마감시간 자동 동기화
- [x] 일정 표기(`오늘/내일/D-##`)를 생성/표시 흐름 전반에 일관 적용
- [ ] 닫기/생성 후 포커스 복귀와 스크롤 잠금 해제 정상화

### 7) 나머지 세부 요소 (`/docs/ui/ui.png`)
- [x] 탭 활성 상태(`홈(HOME)`) 색상 대비와 아이콘 강조 유지
- [x] 혼합 언어 라벨에서 폰트 크기/행간 불일치 최소화
- [x] 설명성 장식 요소가 본문 CTA 가독성을 해치지 않도록 유지

## Common Interaction Checklist
- [ ] `home.expanded`: 홈 탭 + 첫 카드 확장 + 타이머 표시 상태 유지
- [ ] `home.modalOpen`: 퀘스트 추가/편집 모달 오픈 상태 관리
- [x] 실행 중 청크 증감 버튼이 한 줄(`-5/-1/+1/+5`)로 노출되고 충돌 없이 동작
- [ ] 탭 전환/아코디언/체크/모달 이벤트 간 상태 충돌 없음
- [ ] 모든 인터랙션 요소 터치 타깃 `44x44px` 이상

## Validation Gate
1. [x] `npm run typecheck`
2. [x] `npm run lint`
3. [x] `npm run test:mvp`
4. 수동 QA (`390px/430px/768px+`): 헤더/상태/입력/현재퀘스트/대기퀘스트/모달/탭바
5. 접근성 QA: 포커스 순환, 색 대비, 터치 타깃, 모달 종료 후 포커스 복귀
